pipeline {
    agent {
        docker {
            image 'maven:3.9-alpine' 
            args '-v /root/.m2:/root/.m2' 
        }
    }
    stages {
        stage('Build') { 
            steps {
			echo 'till here'
                sh 'mvn -B -DskipTests clean package' 
            }
        }
    }
}